# Contact (`/contact`)


Two-column at `lg`+: left = address, phone (clickable `tel:`), email (clickable `mailto:`), office hours; right = contact form.

Map embed (Google Maps iframe; lazy-load).

Form fields: Name, Phone, Email, Subject, Message. Same validation rules as Admissions.

