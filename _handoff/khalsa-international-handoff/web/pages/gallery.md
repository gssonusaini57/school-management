# Gallery (`/gallery`)


Year tabs (2024 · 2025 · 2026). Within each year: a CSS grid masonry of photos with bilingual alt text. Click photo → modal lightbox with caption (bilingual). Keyboard: arrows navigate, Esc closes.

Components: `Tab`, `Modal` (lightbox), `Image` with intrinsic dimensions for CLS.

