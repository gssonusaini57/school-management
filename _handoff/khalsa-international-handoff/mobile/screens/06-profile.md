# 06 — Profile


Header card: avatar, child's name, class · section · house (`HouseTag`).
Sections:
- Student info (Student ID, DOB, blood group)
- Family (`mobile.profile.father`, `mother`, contact)
- App settings (Language switcher, Notifications toggles)
- Sign out (`Button` variant="danger")

