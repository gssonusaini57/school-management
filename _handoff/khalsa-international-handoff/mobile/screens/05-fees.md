# 05 — Fees


Top: outstanding card — large amount, `mobile.fees.dueDate`, primary `Pay` button → Razorpay sheet.
History: list of `FeeRow`s with `StatusPill` (Paid). Tap → PDF preview, "Download receipt".
Empty: bilingual "No outstanding fees".
Error: bilingual generic.

