# 01 — Welcome


Greeting `mobile.welcome.greetingFull` (`ੴ ਸਤਿ ਸ੍ਰੀ ਅਕਾਲ`) in display type. Beneath: `mobile.welcome.tagline`. Single `Button` "Sign in with phone" → OTP modal.

States: default, OTP-sending (loading), OTP-failed (error toast).
A11y: greeting wrapped `<Text accessibilityRole="header">` with bilingual `accessibilityLabel`.

