# 02 — Home


Top: `MobileTopBar` with school crest + bell.
Sections:
1. Greeting card — `Sat Sri Akal, {parentName}`
2. **Today's timetable** — first 3 `TimetableSlot`s + "View all" → Timetable tab
3. **Fees** — outstanding amount card with `Pay now` CTA. Hidden when paid.
4. **Latest notice** — top `NoticeCard`.
5. **Quick links** — grid of 4 (Attendance, Result, Calendar, Bus).

States: loading skeletons; empty (no student linked); error.

