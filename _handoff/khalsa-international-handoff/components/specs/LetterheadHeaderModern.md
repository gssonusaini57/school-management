# LetterheadHeaderModern

> **Kind:** Print primitive
> Token-only styling. No raw hex / px / font-family literals.


Format B — modern asymmetric. Crest left at 70px, two-column right side: top row = school name + lockup; bottom row = address + contact line aligned in `typography.caption`.

