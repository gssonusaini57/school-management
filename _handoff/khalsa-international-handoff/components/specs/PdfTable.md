# PdfTable

> **Kind:** Print primitive
> Token-only styling. No raw hex / px / font-family literals.


Bordered table for receipts, marks, payslips. Header row: `color.surface.sunken` bg, `typography.button` weight. Cell padding: 6px 8px. All inner borders `color.border.subtle`.

