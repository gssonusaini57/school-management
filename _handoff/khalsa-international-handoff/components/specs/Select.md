# Select

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


Native `<select>` styled to match `<Input>`. Use a custom listbox (`@radix-ui/react-select`) only when grouping or rich items are needed; document the swap. `label`, `hint`, `error` props identical to Input.

