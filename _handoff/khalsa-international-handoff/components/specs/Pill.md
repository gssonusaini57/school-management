# Pill

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


Like Badge but `radius.full`. Used for filter chips and status indicators (e.g. "Paid", "Due"). Can be interactive with `onPress`.

