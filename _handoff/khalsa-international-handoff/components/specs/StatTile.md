# StatTile

> **Kind:** Molecule
> Token-only styling. No raw hex / px / font-family literals.


Used in StatsStrip (home page).

```
┌──────────────────┐
│ 1,240            │  ← typography.headingXl, color.text.primary
│ Students         │  ← typography.caption, color.text.secondary, uppercase
└──────────────────┘
```

Props: `value` (string or number), `label` (i18n string), `accent?` (color token).

