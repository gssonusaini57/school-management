# Checkbox

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


20×20 box, `radius.sm`, border `color.border.strong`. Checked: bg `color.surface.brand`, white tick. Disabled: opacity 0.4. Always paired with a `<label>`. Group multiple with a `<fieldset>` and `<legend>`.

