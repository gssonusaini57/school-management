# HeroBlock

> **Kind:** Organism
> Token-only styling. No raw hex / px / font-family literals.


Home-page hero. Two-column at `lg`+: left = eyebrow + bilingual headline + body + CTA pair; right = a single hero image with the crest watermark in the corner.

At `md` and below: stacked. Image becomes a 16:9 banner above the text.

