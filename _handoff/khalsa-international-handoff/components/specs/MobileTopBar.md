# MobileTopBar

> **Kind:** Organism
> Token-only styling. No raw hex / px / font-family literals.


Top bar 56px. Title centered (`typography.headingSm`). Optional left back button, optional right action (e.g. notification bell with red dot when unread).

