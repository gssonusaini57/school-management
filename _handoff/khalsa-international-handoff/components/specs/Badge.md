# Badge

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


Small inline tag. Height `spacing.6` (24px). `radius.sm`. `typography.caption`. Variants: `brand`, `gold`, `success`, `warning`, `danger`, `neutral`. Use for metadata (e.g. "PSEB", "Class X"), not interactive.

