# Radio

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


20×20 circle, border `color.border.strong`. Selected: 8px inner dot `color.surface.brand`. Always inside `<RadioGroup>` (a `<fieldset>` wrapper).

