# Avatar

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


Circular image. Sizes: 24, 32, 40, 48, 64. Falls back to initials in `typography.button` on `color.surface.brand`. Prop `name` (string) used for initials and `alt`.

