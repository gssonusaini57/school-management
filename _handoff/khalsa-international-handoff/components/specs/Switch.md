# Switch

> **Kind:** Atom
> Token-only styling. No raw hex / px / font-family literals.


Track 32×20, thumb 16×16. Off: track `color.neutral.300`. On: track `color.surface.brand`. `role="switch"`, `aria-checked`. Animation: `motion.duration.base` / `motion.easing.standard`.

