[project]
name = "school-management-backend"
version = "0.1.0"
description = "FastAPI backend for KIS School Management Portal"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
